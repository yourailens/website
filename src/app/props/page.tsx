import type { Metadata } from "next";
import PropsExperience from "./PropsExperience";
export const metadata: Metadata = { title: "Props Library | YourAI Lens Studio", description: "Browse AI reference sheets for props, accessories, and objects across every style." };
export default function PropsPage() { return <PropsExperience />; }
